name: photo_viewer_app
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^5.0.0
  image_picker: ^0.8.4+4
  photo_view: ^0.14.0
  ffi: ^1.1.2
  file_picker: ^4.2.5
  path_provider: ^2.0.11  # 添加这一行

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/deploy.prototxt
    - assets/res10_300x300_ssd_iter_140000.caffemodel
    - assets/arcface_model.onnx
    - assets/face_recognition.dll  # 添加这一行